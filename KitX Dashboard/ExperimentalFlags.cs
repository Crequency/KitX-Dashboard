namespace KitX.Dashboard;

/// <summary>
/// Experimental feature flags. All members were either promoted to real features or
/// removed (D13.3: EnablePluginLaunchWindowWidthSnap deleted as a dead switch that was
/// permanently false). Reintroduce a flag here only while actively developing it.
/// </summary>
public static class ExperimentalFlags
{
}
